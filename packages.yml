packages:
  - package: dbt-labs/dbt_utils
    version: ">=1.0.0"
    # Provides useful helper macros (like surrogate keys, unions)

  - package: metaplane/dbt_expectations
    version: ">=0.10.0"
      # Provides data quality tests (like not null, valid values)
  
  - local: dbt_macro_utils # Custom reusable macro package
  - package: dbt-labs/dbt_project_evaluator
    version: ">=0.8.0"
    # Provides macros for testing and debugging dbt projects
# Run this command after adding packages:
# dbt deps
