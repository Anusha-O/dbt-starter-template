packages:
  - package: dbt-labs/dbt_utils
    version: ">=1.0.0"
    # Provides useful helper macros (like surrogate keys, unions)

  - package: calogica/dbt_expectations
    version: ">=0.10.0"
      # Provides data quality tests (like not null, valid values)
  # Custom reusable macro package
  - local: dbt_macro_utils
# Run this command after adding packages:
# dbt deps
